import { createFile, deleteFile } from './file';
import { writeJSONToFile, readJSONFromFile } from './json';
import { pathExist } from './path';

export default {
  createFile,
  deleteFile,
  writeJSONToFile,
  readJSONFromFile,
  pathExist
};
